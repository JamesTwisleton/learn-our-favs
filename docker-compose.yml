services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: learnmyfavs
      POSTGRES_PASSWORD: learnmyfavs
      POSTGRES_DB: learnmyfavs
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
